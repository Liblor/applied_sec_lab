{
  "iMovies": {
    "ConnectionString": "server={{ groups['dbservers'][0] }};port=3306;database=imovies;user={{ ansible_hostname }};password={{ lookup('vars', 'mysql_' + ansible_hostname + '_password') }}"
  },
  "iMoviesCA": {
    "ConnectionString": "server={{ groups['dbservers'][0] }};port=3306;database=imovies_ca;user={{ ansible_hostname }};password={{ lookup('vars', 'mysql_' + ansible_hostname + '_password') }}"
  }
}
