{
  "iMovies": {
    "ConnectionString": "server={{ DB_SERVER }};port={{ DB_PORT }};database={{ DB_IMOVIES_NAME }};user={{ DB_USER }};password={{ DB_PASSWORD }}"
  },
  "iMoviesCA": {
    "ConnectionString": "server={{ DB_SERVER }};port={{ DB_PORT }};database={{ DB_IMOVIES_CA_NAME }};user={{ DB_USER }};password={{ DB_PASSWORD }}"
  }
}
