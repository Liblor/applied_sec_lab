---
- name: Create folder for user secrets
  file:
    path: "/home/{{ CORECA_UNAME }}/.microsoft/usersecrets/{{ CERTSERVER_USER_SECRETS_ID }}"
    state: directory
    owner: "{{ CORECA_UNAME }}"
    group: "{{ CORECA_UNAME }}"
    mode: 0755

- name: Include mysql variables
  include_vars:
    file: /vagrant/mysql_passwords.yml

- name: Store user secrets template
  template:
    src: secrets.json
    dest: "/home/{{ CORECA_UNAME }}/.microsoft/usersecrets/{{ CERTSERVER_USER_SECRETS_ID }}/secrets.json"
    owner: "{{ CORECA_UNAME }}"
    group: "{{ CORECA_UNAME }}"
    mode: 0644
