These files do not need to be secure.
We pre-generate them to speed up the setup of our environment.
Use the following command to generate more parameters.

```bash
openssl dhparam -out dh_param-x.pem 4096
```
